{{view "select"
       viewName='userSelect'
       classNames="userfilter"
       content=model
       optionValuePath="content.name"
       optionLabelPath="content.name"
       value=userSelected
       prompt='Users - all'
}}
