{{view "select"
       viewName='tagSelect'
       classNames="tagfilter"
       content=model
       optionValuePath="content.name"
       optionLabelPath="content.name"
       value=tagSelected
       prompt='Tags - all'
}}
